import ChangePassword from "../pages/ChangePassword";
import UserProfile from "../pages/Profile/UserProfile";

const min_authorize_pages = {
  profile: UserProfile,
  "change-password": ChangePassword,
};

export default min_authorize_pages;
